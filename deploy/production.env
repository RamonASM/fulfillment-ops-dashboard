# Production Environment Configuration
# Copy this file to /var/www/inventory/.env on your server

# =============================================================================
# REQUIRED - Must be set before deployment
# =============================================================================

# Database (PostgreSQL)
DATABASE_URL="postgresql://inventory_user:YOUR_SECURE_PASSWORD_HERE@localhost:5432/inventory_db?schema=public"

# JWT Secrets (generate with: openssl rand -base64 32)
JWT_SECRET="GENERATE_A_SECURE_SECRET_HERE"
JWT_REFRESH_SECRET="GENERATE_ANOTHER_SECURE_SECRET_HERE"

# Session Secret (generate with: openssl rand -base64 32)
SESSION_SECRET="GENERATE_A_THIRD_SECURE_SECRET_HERE"

# =============================================================================
# Server Configuration
# =============================================================================

NODE_ENV=production
PORT=3001

# Domain Configuration
API_URL=https://api.yourtechassist.us
ADMIN_URL=https://admin.yourtechassist.us
PORTAL_URL=https://portal.yourtechassist.us

# CORS Origins (comma-separated)
CORS_ORIGINS=https://admin.yourtechassist.us,https://portal.yourtechassist.us

# =============================================================================
# Redis (for caching, sessions, job queue)
# =============================================================================

REDIS_URL=redis://localhost:6379

# =============================================================================
# Email Configuration (optional but recommended)
# =============================================================================

# SMTP Settings
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Email sender info
EMAIL_FROM_NAME="Inventory Intelligence"
EMAIL_FROM_ADDRESS=noreply@yourtechassist.us

# =============================================================================
# AI Features (optional)
# =============================================================================

# Claude API (for AI insights, summaries)
# ANTHROPIC_API_KEY=sk-ant-...

# OpenAI API (alternative for AI features)
# OPENAI_API_KEY=sk-...

# =============================================================================
# Monitoring (optional)
# =============================================================================

# Sentry error tracking
# SENTRY_DSN=https://...@sentry.io/...

# Log level (debug, info, warn, error)
LOG_LEVEL=info
