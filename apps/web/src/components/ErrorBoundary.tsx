/**
 * ErrorBoundary Components - Re-export from shared UI library
 *
 * This file re-exports the shared error boundary components for backward compatibility.
 * New code should import directly from '@inventory/ui'.
 */

export {
  ErrorBoundary,
  useErrorHandler,
  withErrorBoundary,
} from '@inventory/ui';
