/**
 * Loading Components - Re-export from shared UI library
 *
 * This file re-exports the shared loading components for backward compatibility.
 * New code should import directly from '@inventory/ui'.
 */

export {
  LoadingScreen,
  LoadingOverlay,
  LoadingSpinner,
  PageLoader,
} from '@inventory/ui';
